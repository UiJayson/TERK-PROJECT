id: routing.whatsapp.hours.reception
category: routing_accuracy
description: WhatsApp customer asks opening hours.
input:
  workspace_id: workspace_123
  channel: whatsapp
  provider: whatsapp_business
  message:
    text: "What are your opening hours?"
  sender:
    type: customer
    phone: "+441234567890"
expected:
  selected_agent: reception
  primary_intent: faq
  required_tools:
    - knowledge.search
  forbidden_tools:
    - crm.opportunity.write
  response_requirements:
    - answers_from_knowledge
    - concise_reply
